{% extends 'base.html.twig' %}

{% block title %}Connexion{% endblock %}

{% block body %}
    <h2>Connexion</h2>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Connexion</button>
    {{ form_end(form) }}
{% endblock %}